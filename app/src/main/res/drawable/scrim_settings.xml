<?xml version="1.0" encoding="utf-8"?>
<!--
  Nearly opaque on the left. The settings list is dense text, and any artwork
  showing through behind it reads as a rendering artefact rather than depth.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="0"
        android:startColor="#FA040B18"
        android:centerColor="#E0040B18"
        android:endColor="#00040B18"
        android:centerX="0.52"
        android:type="linear" />
</shape>
