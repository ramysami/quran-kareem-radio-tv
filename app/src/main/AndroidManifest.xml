<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        ONNX Runtime, which runs the Surah recogniser, is built for Android 7.0
        and up. The radio itself runs back to 5.0, so the library's minimum is
        overridden here and the recogniser is simply not offered below 7.0
        (see Recitation.isSupported).
    -->
    <uses-sdk tools:overrideLibrary="ai.onnxruntime" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Only for the Surah recogniser's "listen through the microphone" option,
        which the user picks in Settings; the default listens to the radio's
        own audio and never opens the microphone at all.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Lets the sleep-timer button in the notification open the app's preset
        dialog. Android refuses to start an activity for a media-control button:
        the press arrives as a session callback rather than as a PendingIntent
        sent by the launcher, which is what earns the widget's timer button the
        same start. Holding this permission is one of the few exemptions from
        that rule. It is asked for, never assumed, and the button is the only
        thing in the app that uses it.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- TV devices have no touchscreen and no telephony -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:name=".QuranRadioApp"
        android:allowBackup="true"
        android:banner="@drawable/banner"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.QuranRadio">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|navigation">

            <!-- Android TV launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- Also show on phones/tablets and non-leanback TV boxes -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Host for the sleep-timer dialog the widget opens. Windowless, with
            its own task affinity, so it floats over the home screen without
            ever pulling the player up behind it or landing in Recents.
        -->
        <activity
            android:name=".SleepTimerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|smallestScreenSize|uiMode"
            android:theme="@style/Theme.QuranRadio.Invisible" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|navigation"
            android:theme="@style/Theme.QuranRadio" />

        <service
            android:name=".PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!--
            Lets a media button reach the app after its process is gone — which
            is how the radio comes back when the car's head unit asks the phone
            to play on connect. PlaybackService.onPlaybackResumption supplies
            the stream.
        -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".SleepAlarmReceiver"
            android:exported="false" />

        <!--
            The system's DownloadManager announcing that the speech model has
            arrived, so it can be unpacked even if the app was closed meanwhile.
        -->
        <receiver
            android:name=".recognition.ModelDownloadReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".RadioWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <receiver
            android:name=".RadioWidgetCompactProvider"
            android:exported="false"
            android:label="@string/widget_label_compact">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_compact" />
        </receiver>

    </application>
</manifest>
